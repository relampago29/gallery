specVersion: "v1"
serviceId: "gallery"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gallery-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
